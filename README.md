 inatilize application 
```sh    
    npm init -y  
```

update script
    "dev": "nodemon index.js",
    "start": "node index.js"

initalize git
    git init

add .env & .gitignore file
    add .env & node_modules into .gitignore

install dependencies
        npm install express mongodb dotenv mongoose
        npm install nodemon -D

